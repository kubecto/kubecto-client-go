apiVersion: v1
kind: Pod
metadata:
  name: test-incluster-client-go
spec:
  containers:
  - name: incluster
    image: in-cluster1:latest
    imagePullPolicy: IfNotPresent
    #command: ["/bin/sh", "-c", "sleep 60m"]

